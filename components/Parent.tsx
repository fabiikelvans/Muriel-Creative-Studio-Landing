import React from 'react';
import {Parent} from "../types/typings";

function Parent({url, token, headers}: Parent) {
    return (
        <div>
            Parent
        </div>
    );
}

export default Parent;